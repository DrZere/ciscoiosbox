# ─── GUI ──────────────────────────────────────────────────────────────────────
# This app uses only QtCore, QtGui and QtWidgets. Swapping the line below for
#     PySide6-Essentials>=6.6.0
# drops ~330 MB of unused Qt (WebEngine, 3D, multimedia) and produces a much
# smaller PyInstaller bundle. See "Smaller install" in README.md.
PySide6>=6.6.0
pyqtgraph>=0.13.3
numpy>=1.24.0             # pyqtgraph's array backend

# ─── Device connectivity ──────────────────────────────────────────────────────
netmiko>=4.3.0            # SSH / Telnet / Serial dispatch + prompt handling
paramiko>=3.4.0           # SSH transport underneath netmiko
pyserial>=3.5             # console / COM-port support

# ─── Output parsing ───────────────────────────────────────────────────────────
textfsm>=1.1.3
ntc-templates>=4.0.0      # community TextFSM templates for Cisco show commands

# ─── Secure credential storage ────────────────────────────────────────────────
keyring>=24.3.0           # OS keychain (Keychain / DPAPI / SecretService)
cryptography>=42.0.0      # Fernet fallback vault when no keyring is available

# ─── SNMP monitoring (optional but recommended) ───────────────────────────────
pysnmp>=6.1.0

# ─── Packaging ────────────────────────────────────────────────────────────────
pyinstaller>=6.3.0
